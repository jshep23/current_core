name: example
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:  
  sdk: ^3.11.5

dependencies:
  current_core:
    path: ../

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
